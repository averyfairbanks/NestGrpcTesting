syntax = "proto3"

package blog;

service BlogService {
    rpc FindById(FindByIdRequest) returns (Blog);
}

message FindByIdRequest {
    int32 id = 1;
}

message Blog {
    int32 id = 1;
    string title = 2;
    string author = 3;
    string body = 4; // change to json
}